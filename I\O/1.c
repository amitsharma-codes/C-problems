#include<stdio.h>
#define MSSG "Hello, What's up?\n"

/*The PreProcessor replaces the macro that is #define in the main program and also  removes me :( from the program while converting this program to assembly code*/

int main()
{
	printf(MSSG);
	return 0;
}
